# Generates github-metrics.svg in the repo root and commits it back.
# The README references the file directly so the image is served from
# raw.githubusercontent.com — no third-party host that can 503.
#
# Runs:
#   - On a weekly schedule (Sundays 01:30 UTC)
#   - On every push to main (so README/workflow tweaks take effect immediately)
#   - On manual dispatch (one-click run from the Actions tab)
#
# Token notes:
#   The default GITHUB_TOKEN is enough for everything below. If you create a
#   personal access token (classic) with scopes `repo, read:org, read:user,
#   read:packages` and store it as the secret `METRICS_TOKEN`, the workflow
#   will pick it up automatically and unlock richer plugins (private contrib
#   counts, follow-up stats, traffic, etc.).

name: Metrics

on:
  schedule:
    - cron: "30 1 * * 0"
  push:
    branches: [main]
    paths:
      - ".github/workflows/metrics.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    steps:
      - name: Generate base metrics card
        uses: lowlighter/metrics@latest
        with:
          filename: github-metrics.svg
          token: ${{ secrets.METRICS_TOKEN || github.token }}
          committer_token: ${{ secrets.GITHUB_TOKEN }}
          user: hamzaziizzz
          template: classic
          base: header, activity, community, repositories
          base_indepth: yes
          config_timezone: Asia/Kolkata
          # Plugins below are intentionally limited to the ones that render
          # cleanly with only GITHUB_TOKEN. Without a PAT the language /
          # topics / lines plugins return empty or near-empty data, which
          # leaves visible gaps in the card. Add them back once a
          # METRICS_TOKEN secret is configured (see workflow header notes).
          plugin_isocalendar: yes
          plugin_isocalendar_duration: full-year
          # The isocalendar plugin renders its chart with a hardcoded inline
          # style="margin-top: -130px;" so the heatmap overlaps the row of
          # streak stats above it. Push it back down by overriding that
          # inline style (inline beats external CSS without !important).
          extras_css: |
            svg[viewBox="0,0 480,270"] { margin-top: 16px !important; }
